# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目定位

个人 A 股量化研究与交易系统，目标市场以 A 股为主，兼顾期货、ETF、可转债等个人可交易品种。核心诉求：个人自用、可维护、数据可靠、回测可信、可选模拟/实盘。

详细选型依据见 `docs/量化开源框架调研与落地方案.md`。

## 架构决策（不可轻易改动）

**核心选型：AKQuant 为骨架 + 自研模块补足**

这是经过对比 17 个开源框架后确定的方向，理由：

- **不用单一重平台**：避免 QUANTAXIS/NautilusTrader 等大型框架的维护成本与黑盒回测风险
- **不用 backtrader**：上游停滞（2024-08 后无提交），且 A 股规则全需自补
- **不用 vectorbt/Lean 等**：分别缺实盘或不友好于 A 股
- **不用纯自研**：基础事件循环、订单管理没必要重新造轮子

分层职责（务必遵守）：

| 层 | 实现方式 | 职责 |
|---|---|---|
| 数据采集 | akshare（主）+ adata/Ashare（补） | 行情、财务、实时 |
| 数据存储 | DuckDB + Parquet（自研层） | 复权/停牌/ST 标记 |
| 核心引擎 | **AKQuant 直接使用，不修改其源码** | 事件循环、订单管理、基础回测 |
| A 股补丁 | 自研模块（在 AKQuant 之上） | ST/退市/可转债/涨跌停边界 |
| 因子库 | 完全自研 | 计算、去极值、中性化、标准化 |
| 绩效归因 | 自研 | 含 Walk-Forward 防过拟合 |
| 实盘网关 | 自研（QMT/miniQMT） | 仅在 P3 阶段接入 |

**扩展 AKQuant 时，禁止直接修改其源码**；通过继承、补丁模块、策略层包装实现扩展。如果必须修改，先在 Issue 中讨论并评估是否切换到 rqalpha。

## A 股特殊性（必须严格遵守）

回测或实盘代码涉及交易逻辑时，必须显式处理以下规则，缺一不可：

- **T+1 交割**：当日买入次日才可卖出
- **涨跌停限制**：涨停日不可买入，跌停日不可卖出
- **停牌处理**：停牌日无成交，停牌期间不计入持仓计算
- **除权除息**：以"前复权"为统一口径，跨数据源需校验
- **ST 股票**：默认过滤，除非策略明确包含
- **最小交易单位**：100 股整手（可转债为 10 张）
- **印花税**：卖出单边收取
- **幸存者偏差**：回测样本必须包含已退市股票

通用框架（backtrader/vectorbt/LEAN）**默认不覆盖**这些规则，不能假设"框架已处理"。每次写新策略前，列出涉及到的规则清单。

## 数据源策略

- **主采集**：akshare（与 AKQuant 生态一致）
- **校验源**：adata（免费开源 A 股数据库）
- **实时补**：Ashare（新浪+腾讯双源）
- **交叉校验**：Tushare Pro（积分门槛）/ baostock（备用）
- **存储**：DuckDB（查询）+ Parquet（持久化）
- **统一口径**：以 akshare 复权因子为准，其他源用于差异告警

数据质量要求：

- 每日采集后自动跑质检（前复权口径、缺失日期、异常值）
- akshare 接口易变，若主接口失败自动降级到 adata
- 所有原始数据先入 `data/raw/`，清洗后入 `data/clean/`，不要直接覆盖

## 技术栈（已选定，不要引入其他方案）

| 维度 | 选择 | 禁止 |
|---|---|---|
| Python | 3.11+ | 不要用 3.9 及以下 |
| 包管理 | uv 或 poetry | 不要用 pip + requirements.txt |
| 代码质量 | ruff + mypy | 不要用 flake8/black（已被 ruff 取代） |
| 数据处理 | pandas + numpy | 因子库统一用 pandas |
| 数值计算 | scipy（按需） | 不要为了 ML 引入 PyTorch（除非进入 ML 阶段） |
| 参数优化 | optuna | 不要用 grid search |
| 可视化 | Streamlit + Plotly | 不要用 Flask + Echarts |
| 调度 | APScheduler | 不要用 cron（Windows 不可靠） |
| 日志 | loguru | 不要用标准 logging |
| 告警 | 钉钉 webhook | 个人项目不要接 PagerDuty |
| 测试 | pytest | 不要用 unittest |

## 6 周路线图（执行优先级）

P1（必须完成才能进入 P2）：
1. **第 1 周**：AKQuant 验证 + 双均线策略跑通 + A 股规则测试
2. **第 2 周**：数据层（DuckDB + 多源校验 + 质检）

P2（核心能力）：
3. **第 3 周**：因子库 + AKQuant 策略集成
4. **第 4 周**：A 股特殊规则补丁
5. **第 5 周**：绩效分析与 Walk-Forward 防过拟合

P3（增强）：
6. **第 6 周**：模拟盘 + Streamlit dashboard + 监控告警

完成定义（DoD）：

- P1 完成后才有"可信赖的回测"
- P2 完成后才有"可比较的策略"
- P3 完成后才有"可上线的系统"

## 防过拟合原则（必须遵守）

所有策略在交付前必须通过：

1. **Walk-Forward 验证**：训练 2 年、测试 1 年、季度滚动
2. **样本内外对比**：测试集表现衰减 < 30%
3. **参数敏感度**：最优参数附近 ±20% 范围内表现稳定
4. **简单优先**：同等收益下选择参数更少的策略

**禁止**：
- ❌ 在全样本上优化后直接报告 Sharpe
- ❌ 用未来数据做特征工程
- ❌ 只展示收益最好的几次回测
- ❌ 把过拟合的策略包装成"已验证"

## 项目结构（待创建）

```
quant/
├── config/                          # 配置（数据源/账户/参数）
├── data/                            # 数据层
├── research/                        # 因子库 + 策略原型
├── backtest/                        # 回测层（A 股补丁 + 归因）
├── execution/                       # 模拟盘 + 实盘网关
├── ops/                             # 监控 + 告警 + dashboard
├── tests/                           # 单元测试
├── docs/                            # 调研文档 + 设计文档
└── pyproject.toml
```

## 关键文档

- `docs/量化开源框架调研与落地方案.md`：框架选型调研与原始落地计划
- 本文件已基于该文档收敛了选型决策，新增内容以本文件为准

## 合规与实盘纪律

- 程序化交易需向交易所/券商报备（具体规则随政策变化）
- QMT 有 300W 资金门槛；miniQMT 无门槛但功能受限
- 实盘前必须经过至少 4 周模拟盘验证
- 单策略初始实盘资金不超过总资金 10%
- 所有实盘代码必须经过模拟盘对比验证偏差 < 5%

## 与 Claude Code 协作的约定

- 写新模块前先看 `docs/量化开源框架调研与落地方案.md` 确认是否已有决策
- A 股规则相关代码必须配单元测试（边界：涨停日、跌停日、停牌日、除权日、ST 日、退市日）
- 不要主动建议引入新的依赖，除非确实必要且给出对比理由
- 不要修改 AKQuant 源码；通过继承与包装扩展
- 涉及回测结果的对话，必须明确标注"样本内"还是"样本外"